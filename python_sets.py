# Create an empty set using the set() function
numbers = set()

# Get an integer input from the user
number = int(input("Enter an integer: "))

# Add the number to the numbers set using the add() method
numbers.add(number)

# Print the numbers set
print(numbers)